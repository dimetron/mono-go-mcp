# secrets — never commit
.env
.env.*.backup
*.env.bak

# build output
bin/
dist/
.task/
# stray root-level binaries from a bare `go build ./cmd/...`
/mono-go-mcp
/mono-go-cli

# local-only tool state
.pi-go/cache/
.pi-go/tasks/
.pi-go/.claude/
coverage.out
