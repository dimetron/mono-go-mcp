# Personal token from https://api.monobank.ua/ (required for /personal/* tools).
# Keep this file out of git: .env is git-ignored. Set restrictive permissions:
#   chmod 600 .env
MONO_TOKEN=***
# Optional API base override (tests/proxy). Only HTTPS origins receive X-Token.
# MONO_BASE_URL=https://api.monobank.ua
